# Signature-Detection-YOLO
 Azure Function that detects and extracts handwritten signatures from document images using a custom-trained YOLOv8 model, returning bounding regions and optional base64-encoded crops via HTTP API.
